# Claude repository context

Follow `AGENTS.md` and the central CLARYEL architecture. This repository is public and must remain free of private machine identity, personal data, credentials and private implementation history.

Claude-related integration is documented but not tested with CLARYEL RemoteOps. Do not change that status without dated, repeatable evidence.

Prefer small, reviewable changes. Never introduce direct arbitrary command execution, silent privilege escalation or a path that bypasses plan validation, approval, backup, health checks or rollback.
